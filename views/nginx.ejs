<!DOCTYPE html>
<html>
<head>
    <title>Nginx Configuration</title>
    <!-- Bootstrap for consistent styling with the rest of the app -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    >
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container my-4">
        <h1 class="mb-4">Nginx Configuration</h1>
        <!-- Brief instructions so users know what this page does -->
        <div class="alert alert-info">
            <p class="mb-0">
                Editing <code><%= nginxPath %></code>. After saving, BlockHead will
                attempt to validate the configuration and reload nginx.
            </p>
        </div>
        <!-- Form posts the updated config back to the server -->
        <form method="post" action="/nginx">
            <div class="mb-3">
                <!-- Textarea pre-filled with the current nginx.conf contents -->
                <textarea name="config" class="form-control" rows="20"><%- config %></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Save</button>
            <a href="/" class="btn btn-secondary ms-2">Back</a>
        </form>
    </div>
    <%- include('partials/log-panel') %>
</body>
</html>
